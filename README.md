# MultiThreaded-Recursive-FileSearch
Multithreaded recursive directory scanner that enumerates all subfolders and files starting from a single root path, providing fast counts of directories, files, and total objects.
